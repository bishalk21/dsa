/** Merge Intervals
 * - this pattern is used to deal with overlapping intervals or ranges of values.
 * - it involves merging overlapping intervals into a single interval that covers the entire range of values.
 *
 * Data Structures involved: Arrays, Linked Lists, Heap, etc.
 *
 * The basic idea is to sort the intervals based on their starting points, and then iterate through the sorted intervals to merge any overlapping intervals.
 * This technique is often used to solve problems that involve scheduling, calendar events, or any situation where we need to combine overlapping ranges of values.
 *
 * Example problems that can be solved using the merge intervals technique include:
 * - Merging overlapping intervals in a list of intervals.
 * - Finding the minimum number of meeting rooms required for a given schedule of meetings.
 * - Finding the maximum number of overlapping intervals at any point in time.
 */
