/** Matrix Traversal (Islands)
 * - This pattern describes all the efficient ways of traversing through a matrix or 2D array.
 * - It is often used to solve problems that involve searching for specific elements, counting occurrences, or finding connected components in a grid.
 *
 * Data Structures involved: Arrays, Matrices, Graphs, Queue, etc.
 *
 * The basic idea is to use nested loops to iterate through the elements of the matrix and apply a specific traversal method (e.g., depth-first search, breadth-first search) to explore the neighboring elements.
 * This technique is often used to optimize time complexity from O(n^2) to O(n) in certain problems.
 *
 * Example problems that can be solved using the matrix traversal technique include:
 * - Counting the number of islands in a grid.
 * - Finding the largest connected component in a grid.
 * - Solving maze problems.
 * - cycle in a matrix.
 * - flood fill algorithm.
 */
