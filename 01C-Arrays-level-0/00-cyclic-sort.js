/** Cyclic Sort (also known as Index Sort)
 * Cyclic Sort is an efficient algorithm for sorting an array of integers
 * where the integers are in the range from 1 to n, and there are no duplicates.
 * The algorithm works by placing each number in its correct position in the array.
 * If a number is not in its correct position, it is swapped with the number
 * that is currently in that position. This process continues until all numbers are
 * in their correct positions.
 *
 * Data Structures involved: Arrays
 *
 * The basic idea is to iterate through the array and for each number,
 * check if it is in its correct position. If it is not, swap it with the number
 * that is currently in its correct position. This process continues until all
 * numbers are in their correct positions.
 *
 * Example problems that can be solved using the cyclic sort technique include:
 * - Sorting an array of integers where the integers are in the range from 1 to n.
 * - Finding the missing number in an array of integers where the integers are in the range from 1 to n.
 * - Finding the duplicate number in an array of integers where the integers are in the range from 1 to n.
 */
