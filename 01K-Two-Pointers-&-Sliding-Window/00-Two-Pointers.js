/** Two Pointers
 * Two pointers is a technique that uses two pointers to iterate through an array or string.
 * The pointers can be used to find pairs of elements that satisfy a certain condition, or
 * to find the longest substring without repeating characters, among other things.
 *
 * Data Structures involved: Arrays, Strings, Linked Lists, etc.
 *
 * The basic idea is to use two pointers to represent the current position in the array or string.
 * The pointers can be moved in different directions (e.g., left and right) based on the conditions of the problem.
 * This technique is often used to optimize time complexity from O(n^2) to O(n) in certain problems.
 *
 * Example problems that can be solved using the two pointers technique include:
 * - Finding pairs of elements that sum up to a specific target.
 * - Finding the longest substring without repeating characters.
 * - Merging two sorted arrays.
 * - Reversing a string or an array in place.
 */
