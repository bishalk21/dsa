/** Fast and Slow Pointers (also known as Tortoise and Hare)
 * The fast and slow pointers technique is used to solve problems involving linked lists or arrays where we need to find a specific element or pattern.
 * It involves using two pointers that move at different speeds through the data structure.
 *
 * Data Structures involved: Linked Lists, Arrays, Strings, etc.
 *
 * The basic idea is to use two pointers, one moving faster than the other, to traverse the data structure.
 * This technique is often used to detect cycles in a linked list, find the middle element of a linked list, or solve other problems that can be approached with a two-pointer strategy.
 *
 * Example problems that can be solved using the fast and slow pointers technique include:
 * - Detecting a cycle in a linked list.
 * - Finding the middle element of a linked list.
 * - Removing the nth node from the end of a linked list.
 * - Finding a cycle in a circular array.
 */
